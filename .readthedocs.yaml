version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"
  apt_packages:
    - fonts-noto-cjk
    - fonts-noto-cjk-extra
    - texlive-lang-chinese
    - texlive-xetex
    - texlive-latex-extra
    - latexmk
    - librsvg2-bin

  jobs:
    post_build:
      - sphinx-build -b html source $READTHEDOCS_OUTPUT/html
      - sphinx-build -b latex source _build/latex
      
      # 额外检查：如果 cover.tex 在 source 目录下，拷贝到编译目录
      - cp source/*.tex _build/latex/ || true
      
      # 编译 PDF
      - cd _build/latex && xelatex -interaction=nonstopmode KargoManual.tex
      - cd _build/latex && xelatex -interaction=nonstopmode KargoManual.tex
      
      - mkdir -p $READTHEDOCS_OUTPUT/pdf
      - cp _build/latex/KargoManual.pdf $READTHEDOCS_OUTPUT/pdf/

python:
  install:
    - requirements: requirements.txt

sphinx:
  configuration: source/conf.py